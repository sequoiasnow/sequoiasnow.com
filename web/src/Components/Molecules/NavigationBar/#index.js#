import React from 'react'
import NavigationItem from '../../Atoms/NavigationItem'

/**
 * @molecule
 * @description
 *   A simple stateless component which takes a collection of children and
 *   wraps them to create a navigation bar. Does not preserve state.
 */
export default class NavigationBar extends React.Component {
  static propTypes = {
    children: PropTypes.arrayOf(PropTypes.instanceOf(NavigationItem))
  }

  /**
   * On click of an event, update the state and trigger the onClick 
   * event, however, this only should apply t
   */
  _onClick(index) {
    if ( this.props.onClick ) {
      
    }
  }

  
  render() {
    const { children } = this.props
    return (
      <div>
        {children.map((Child, index) => {
           return <Child key={index} onClick={}/>
         })}
      </div> 
    ) 
  }



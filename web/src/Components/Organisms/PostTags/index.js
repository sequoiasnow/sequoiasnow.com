import React     from 'react'
import PropTypes from 'prop-types'

/**
 * Used in conjuncture with the PostExcerpt and the 
 * tag container, creates a tag container, when a tagged is clicked,
 * the page will develope a crack, using the Crack component, and 
 * relavent posts for that tag will be displayed 
 */
